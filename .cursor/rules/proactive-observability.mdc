---
description: 默认主动加可观测性/定位手段，不等用户催促
alwaysApply: true
---

# 可观测性：主动做，不等「踢屁股」

在实现或修改**任何可能影响线上排障**的路径时，应**默认**把定位手段做进去，而不是等用户要求再加。

## 何时必须考虑

- 新建或改动 **HTTP API**（尤其非幂等、长耗时、流式响应）
- **外部依赖**（LLM、GitHub、第三方 HTTP、WebSocket、上传解析）
- **认证 / 额度 / 限流** 相关分支
- **客户端 + 服务端** 成对逻辑（错误信息应对齐同一追踪 ID）

## 做到什么程度（按优先级）

1. **关联 ID**：请求级 `X-Request-Id`（或等价）在服务端接受/回显；客户端失败时把 ID 带进错误与控制台日志。
2. **结构化日志**：关键阶段用单行 JSON 或稳定前缀（如本仓库 `[GITLINK] …`），含 `phase`、`elapsedMs`、与业务相关的计数/布尔（**绝不**打 token、密码、整段 body）。
3. **长连接 / 流**：首字节尽快写出；长时间无业务进度时 **heartbeat** 或等价保活；记录 `close`/`finish`、背压、已写字节/行数。
4. **客户端**：失败路径附带可量化诊断（HTTP、Content-Type、分片读取次数、字节、解析路径、部署版本头若存在），便于与服务器日志对表。
5. **已有模式**：同类功能先 grep 仓库内现成做法（例如 `linkSearch` / `CodernetLinkSearchClientDiag`），**对齐**命名与字段，避免各写一套。

## 自我检查（任务结束前过一遍）

- 若此功能明天线上出问题，**仅凭日志 + 用户截图**能否在 10 分钟内缩小到「客户端 / 网关 / 服务端 / 第三方」之一？
- 若不能，**补最小集**日志或诊断字段，直到能回答上述问题。
