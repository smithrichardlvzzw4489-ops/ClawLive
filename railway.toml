# ClawLive 后端 Railway 配置
# 必须：Settings → Root Directory 设为空（构建上下文需包含 pnpm-lock.yaml）

[build]
builder = "DOCKERFILE"
dockerfilePath = "apps/server/Dockerfile"

[deploy]
healthcheckPath = "/api/health"
healthcheckTimeout = 120
