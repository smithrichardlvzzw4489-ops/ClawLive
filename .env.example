# Database
DATABASE_URL="postgresql://clawlive:password@localhost:5432/clawlive"
POSTGRES_USER=clawlive
POSTGRES_PASSWORD=your_password_here
POSTGRES_DB=clawlive

# Redis
REDIS_URL="redis://localhost:6379"

# JWT
JWT_SECRET="your-super-secret-jwt-key-change-in-production"
JWT_REFRESH_SECRET="your-super-secret-refresh-key"
JWT_EXPIRES_IN="24h"

# 管理接口密钥（/api/admin/*）：如 POST /api/admin/grant-litellm-key（线上给用户发 LiteLLM 虚拟 Key，无需本机脚本）
# 验证方式：Header X-Admin-Secret 或 query ?secret=
# ADMIN_SECRET=your-long-random-secret

# Darwin 默认名称：微信小程序登录自动创建 Lobster 实例、以及免问卷申请时使用（可改为中文昵称等）
# DEFAULT_DARWIN_NAME=DarwinClaw

# Telegram OAuth (optional)
TELEGRAM_BOT_TOKEN="your_telegram_bot_token"
TELEGRAM_BOT_USERNAME="your_bot_username"

# Server
SERVER_PORT=3001
NODE_ENV=development

# Frontend URLs (for Next.js)
NEXT_PUBLIC_API_URL="http://localhost:3001"
NEXT_PUBLIC_SOCKET_URL="http://localhost:3001"
# Vercel 部署 web 时：NEXT_PUBLIC_API_URL 须为公网 https（勿用 localhost），否则 /api 代理会触发 DNS_HOSTNAME_RESOLVED_PRIVATE
# NEXT_PUBLIC_API_URL="https://api.your-domain.com"
# VibeKids 已合并进 apps/web（路由 /vibekids），无需单独子应用或 VIBEKIDS_PROXY_ORIGIN
# 已登录用户创作室「生成作品」会请求 NEXT_PUBLIC_API_URL/api/lobster/vibekids-generate（Darwin + LiteLLM 虚拟 Key）；「智能拓展 3 方案」请求 vibekids-brainstorm；近期线索落库 vibekids_darwin_memory。未登录仍走 Next 上 OpenRouter/演示
# VibeKids 生成/修改墙钟默认 300s（Darwin 与 Next OpenRouter 一致），可用 VIBEKIDS_GENERATE_DEADLINE_MS（10000～600000）覆盖。Next 访客路径上 OPENROUTER_TIMEOUT_MS 可在 5000～该墙钟之间微调单次 HTTP；OPENROUTER_MAX_ATTEMPTS 默认 2，总耗时仍不超过墙钟。浏览器 fetch 默认同源截止+15s，可用 NEXT_PUBLIC_VIBEKIDS_CLIENT_FETCH_MS（≥20000）覆盖
# VIBEKIDS_GENERATE_DEADLINE_MS=300000
# NEXT_PUBLIC_VIBEKIDS_CLIENT_FETCH_MS=315000
# Vercel 上未设置时 VibeKids 额度/作品 JSON 会写在临时目录（冷启动可能清空）；持久化可设例如 VIBEKIDS_DATA_DIR（须为可写路径）
# VIBEKIDS_DATA_DIR=
# 多实例/Serverless 推荐：Upstash Redis（REST），与 Vercel 同区域；设任一组即可优先于本地文件
# VIBEKIDS_UPSTASH_REDIS_REST_URL="https://xxxx.upstash.io"
# VIBEKIDS_UPSTASH_REDIS_REST_TOKEN="..."
# 或与现有 Upstash 复用：
# UPSTASH_REDIS_REST_URL=
# UPSTASH_REDIS_REST_TOKEN=
# 是否展示直播相关入口与页面（不设或 false = 隐藏；true = 显示）
# NEXT_PUBLIC_SHOW_LIVE_FEATURES=true

# OpenClaw Integration
WEBHOOK_SECRET="your-webhook-secret-for-verification"

# GitHub API：Darwin 进化器与 search_github_skills 检索 SKILL.md；不设则匿名易限流
# GITHUB_TOKEN="ghp_xxxx"  # 或 GH_TOKEN

# Darwin 全网能力（可选）
# TAVILY_API_KEY=        # web_search
# JINA_API_KEY=          # url_read（Jina Reader，不设也可用免费档）
# EXA_API_KEY=           # exa_search 语义搜索（与 Agent Reach / Exa 对齐）
# AUTOCLI_API_BASE=https://www.autocli.ai   # Darwin autocli stats（默认官方；自建兼容端可改）
# OPENCLI_RS_BIN=/usr/local/bin/opencli-rs  # 服务端执行 opencli-rs（autocli action=run）；不设则仅 stats/help

# LLM：若已配置下方 LiteLLM，则站内摘要等优先走代理；否则回退 OpenRouter
# OPENROUTER_API_KEY="sk-or-xxxx"
# OPENROUTER_MODEL="anthropic/claude-opus-4.6"

# 虾米积分 → LiteLLM 代理（虚拟 Key + 预算）；未配置时兑换与连接测试不可用
# LITELLM_BASE_URL="http://localhost:4000"
# LITELLM_MASTER_KEY="sk-..."
# 展示给前端的代理根地址（可与 BASE 相同，或公网域名）
# LITELLM_PUBLIC_BASE_URL="https://llm.example.com"
# POINTS_PER_USD=1000
# MIN_REDEEM_POINTS=100
# LITELLM_MODELS="gpt-4o,gpt-4o-mini"
# 服务端生成作品摘要等使用的模型（可选，默认取 LITELLM_MODELS 第一个）
# LLM_SUMMARY_MODEL="gpt-4o-mini"

# Upload (optional, for screenshots)
UPLOAD_MAX_SIZE=10485760
CDN_URL=""
